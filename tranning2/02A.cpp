#include<stdio.h>
#include<iostream>

using namespace std;

int main(){
	do{
		int n;
		cin >> n;
		if(n < 0) break;
		int a[n], maxa = 0;
		for(int i = 0; i < n; i++){
			cin >> a[i];
			if(a[i] > maxa) maxa = a[i];
			}
		for(int i = 0; i < n; i++){
			
			}
	} while(1);
	}
